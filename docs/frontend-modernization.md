# 前端现代化说明

当前前端是 Vue 2、Vue CLI 和 Element UI。本项目暂不直接重写阅读器和管理功能。后续候选为 Vue 3 + Vite + Element Plus/其他许可证明确的组件方案。

参考 LegadoTeam/legado 仅用于 API 行为和交互研究，不复制 Android UI、书源集合、图片或未核验代码。迁移将先建立 API 兼容层，再按书架、搜索、书源、导入、用户设置逐页替换，并保留现有用户数据。
