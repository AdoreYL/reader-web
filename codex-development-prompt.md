# reader-web：Codex 自动开发与 Debian 一键部署提示词

> 本文件是交给 Codex 阅读的项目总指令，不是 PowerShell、Bash 或 Python 脚本，不能在终端直接执行。
>
> 用户不是开发人员。除最终在 Debian 服务器复制并执行一条安装或更新命令、以及将完整终端输出粘贴回来以外，用户不负责安装 Java、Node、Docker、Gradle、Git，不负责阅读报错、排查环境或手动修改配置。所有开发、构建、测试、Git 提交、GitHub 推送、安装脚本和文档必须由 Codex 完成。

## 立即执行指令

请从现在开始独立开发并交付可部署的 `AdoreYL/reader-web`，不要只输出审计报告、计划、环境要求或需要用户执行的开发命令。先阅读已有源码和文档，随后自行实施、测试、提交并推送代码。

可以自行做出的决定直接做，不需要逐项向用户确认。只有以下情况才能暂停并向用户提问：

1. 必须提供但无法自行得知的生产信息，例如域名、DNS 是否已解析、邮箱、服务器 IP、是否保留旧线上数据；
2. 安装脚本或服务器日志表明需要用户完成不可替代的外部操作，例如域名 DNS 配置、云厂商防火墙放行 80/443、输入验证码；
3. 会删除或覆盖用户已有生产数据，且无法在脚本中先备份并明确提示。

不要因为 Windows 本机没有 JDK、Node、Docker、Gradle 或某个命令执行器暂时不可用，就把安装环境工作交还给用户。应优先使用项目 Docker 构建、GitHub Actions、容器化工具链、隔离临时目录或可恢复的替代方案完成工作，并如实记录尚未能验证的内容。

## 已确认路径与 Git 远程

- 实际源码目录：`F:\Code\reader-web-source`
- 本提示词和需求文档目录：`F:\Code\reader-web`
- 新项目远程 `origin`：`https://github.com/AdoreYL/reader-web.git`
- 原始服务端远程 `upstream`：`https://github.com/AdoreYL/reader.git`
- Legado 社区参考远程 `legado-upstream`：`https://github.com/LegadoTeam/legado.git`
- 历史/分支参考：
  - `https://github.com/gedoor/legado.git`
  - `https://github.com/HapeLee/legado-with-MD3`
  - `https://github.com/mgz0227/legado-Harmony`
  - `https://github.com/Luoyacheng/legado-E`

当前工作基线来自 `AdoreYL/reader`，现有源码历史必须保留。`origin` 已是用户拥有的空仓库。你已获得授权：

- 在 `F:\Code\reader-web-source` 修改和新增项目文件；
- 将当前迁移基线推送到 `origin`；
- 创建本地提交，并将已验证的开发成果推送到 GitHub；
- 读取上述公开上游仓库和发布所需公开依赖。

禁止向 `upstream`、`legado-upstream` 或其他参考仓库推送。不得重写公开历史或强制推送，除非用户明确要求。

## 用户最终只需要做什么

最终交付必须让用户只需在一台干净的 Debian 服务器以 root 或具备 sudo 权限的用户执行一条命令。例如最终命令应采用以下形式，具体路径按实际发布文件确定：

```bash
curl -fsSL https://raw.githubusercontent.com/AdoreYL/reader-web/main/scripts/install-debian.sh | sudo bash
```

安装脚本可以用清晰的中文提示向用户询问无法预先获得的信息，例如域名和管理员邮箱。对于管理员初始密码，优先自动生成高强度随机密码并在安装结束时只显示一次；禁止把默认密码、Token 或邀请码硬编码到仓库、镜像、日志或 GitHub Actions。

安装后必须打印：

- 访问地址；
- 管理员用户名；
- 一次性显示的初始管理员密码或安全设置下一步；
- 更新命令；
- 查看运行状态和收集诊断日志的一条命令；
- 数据和备份所在位置。

当用户粘贴 Debian 安装或运行日志时：

1. 先根据日志定位实际失败阶段；
2. 不要泛泛要求用户自行检查 Java、Docker、端口或 DNS；
3. 能通过修改仓库、安装脚本或 Docker 配置解决的，直接修复、测试、提交并推送；
4. 然后只给用户下一条需要执行的更新或诊断命令；
5. 解释应使用简体中文、短句，并说明“命令做什么”和“把全部输出发回来”。

## 产品目标

在不修改 `AdoreYL/reader` 原仓库的前提下，开发 `AdoreYL/reader-web`：一个面向 PC 和手机浏览器、可长期运行在 Debian/Docker/NAS 上的私有 Web 阅读服务。

目标能力：

1. 保留并逐步验证传统 Legado 核心书源能力：搜索、详情、目录、正文、XPath、JSONPath、Jsoup CSS Selector、正则、内嵌 JavaScript；
2. 支持书源导入、预览、去重、批量管理、校验和调试；
3. 支持书架筛选、批量管理、分组、换源、阅读进度和书签；
4. 支持管理员与普通用户，按用户隔离书源、书架、分组、进度、书签、设置、本地书和缓存；
5. 支持安全公网部署、备份、恢复、升级和回滚；
6. 保留需要兼容的 `/reader3/*` API，并以测试记录兼容范围；
7. 应用实际监听且容器内部端口固定为 `6788`，公网 HTTPS 只通过 Caddy 或 Nginx 反向代理提供。

不开发 Android、桌面客户端，也不直接合并 Android UI 代码。

## 交付策略：先可安装、再持续完善

这是大型重构，不要等全部理想功能完成才推送。以能在 Debian 一键安装和稳定更新的可运行版本为优先级，持续交付小而完整的垂直切片。

### 第一个可部署版本必须具备

1. 从 `AdoreYL/reader` 迁移的完整 Git 历史并已推送至 `AdoreYL/reader-web`；
2. 一个唯一、可重复的 Docker Compose 主入口；
3. 应用运行在容器内部端口 `6788`；应用服务不直接发布宿主机端口；
4. Caddy 或 Nginx 独立反向代理；有域名时自动申请 HTTPS；
5. `scripts/install-debian.sh`、`scripts/update-debian.sh`、`scripts/diagnose-debian.sh`；
6. 独立持久化数据卷、配置目录、日志目录和可验证的健康检查；
7. `.env.example`，不含真实密码、Token、域名或邮箱；
8. 安全的首次管理员初始化，生产默认关闭公开注册；
9. 用户可运行的一条安装命令、一条更新命令和一条诊断命令；
10. README 中完整的中文安装、更新、备份、恢复和卸载说明；
11. GitHub Actions 至少构建并检查 Docker 镜像/Compose 配置，避免把明显不能构建的代码推送给用户；
12. 保留上游 GPLv3 许可证、版权声明和必要 NOTICE，不以闭源方式重新发布。

如果某个旧功能尚不能在新安全模型下交付，必须在 UI/API 中明确显示不可用或迁移中，不能伪装为已支持。

### 后续迭代优先级

按以下顺序持续推进，无需等待用户理解每个技术细节：

1. 构建、Docker、Debian 安装、HTTPS、健康检查、备份和升级回滚；
2. 认证、用户隔离、管理员后台与基础审计；
3. 书源导入/管理、统一 SSRF 防护、调试和批量校验；
4. 书架、分组、阅读、换源、缓存和移动端体验；
5. `/reader3/*` 契约测试和传统 Legado 规则回归集；
6. 依赖/JDK 现代化与 Vue 3/Vite 前端渐进迁移；
7. 深化限流、可观测性、备份恢复演练和管理员运维功能。

不要在第一个版本一次性重写所有 Vue、Spring、Kotlin、Gradle、数据层和 API。每次推送必须保持可部署、可回滚。

## Debian 部署硬性要求

目标系统为 Debian。安装脚本需要自行检测当前版本、CPU 架构、root/sudo 权限、可用磁盘、Docker/Compose、端口占用和网络连通性，并在需要时以 Debian 官方推荐方式安装 Docker Engine 和 Docker Compose 插件。

安装器必须：

- 支持 Debian 12，尽可能兼容受支持的 Debian 11；不支持时用中文说明真实原因；
- 默认把项目安装到明确目录，例如 `/opt/reader-web`，持久化数据放在明确受保护目录，例如 `/opt/reader-web/data`；
- 安装前检查既有安装；重复运行必须是安全的更新/修复流程，不得无提示删除数据；
- 更新前自动生成带时间戳的本地备份，失败后提供明确回滚路径；
- 不使用 Watchtower，不挂载 Docker Socket 给应用容器；
- 不在 Compose 文件中硬编码管理员密码、邀请码、Cookie、Token 或私钥；
- 如果未提供域名或 DNS 未解析，明确提示用户设置域名并提供检查结果；不能为了“安装成功”关闭 TLS、暴露应用端口或改用不安全默认值；
- 如果选择无域名的本地/LAN 模式，必须显式标记为非公网模式，且限制监听地址或要求额外确认；
- 为反向代理暴露必要的 `80`/`443`，应用容器仅加入内部 Docker 网络；
- 使用健康检查等待应用真正就绪，失败时自动打印 `docker compose ps`、关键服务日志和下一步诊断命令；
- 支持 `--dry-run` 或等价预检模式，不改变服务器状态；
- 支持非交互参数或环境变量，方便之后实现真正无人值守部署，但默认交互提示必须清晰。

只在仓库中交付文本脚本和文档，不把服务器日志、真实 `.env`、备份文件或用户数据提交到 Git。

## 安全与数据隔离硬性要求

当前上游可能存在 URL 传 Token、`userNS` 切换数据域、弱密码哈希、关闭 TLS 校验、宽松 CORS、用户名作为目录名和非原子 JSON 写入等历史问题。它们必须在迁移中逐项淘汰，不能因“兼容旧版”保留为生产默认行为。

### 认证与授权

- 角色仅为 `ADMIN` 与 `USER`；首次管理员初始化只允许安全执行一次；
- 注册模式为 `CLOSED`、`OPEN`、`INVITE_ONLY`，生产默认 `CLOSED`；
- 密码采用当前安全、带盐的慢哈希算法；不得继续使用 MD5；
- Token、管理密钥、密码和 Cookie 不得放入 URL、前端持久化存储、日志或错误响应；
- 使用安全 Cookie 或标准 `Authorization` Header；Cookie 使用 `HttpOnly`、`Secure`、合适的 `SameSite`，并采用匹配的 CSRF 防护；
- 普通用户不得以 `userNS`、用户名、userId 或路径参数选择其他数据域；管理员代管必须使用专用后台操作并写入审计日志；
- 服务端是唯一权限边界，前端隐藏按钮不是授权实现。

### 数据与文件

- 以不可枚举内部 `userId` 作为数据目录标识，绝不直接把用户名、路径参数或压缩包条目拼到文件路径；
- 书源、书架、分组、阅读进度、书签、设置、本地书、缓存和导出都必须按用户隔离；
- JSON 存储保留期间，实现格式版本、原子写入、并发控制、损坏检测、备份和恢复；SQLite 迁移只能作为独立、可回滚任务；
- 所有上传、导入、解压、导出和 WebDAV 路径必须做规范化与根目录边界校验；
- 任何数据迁移先备份、支持预演、输出迁移报告，并可回滚。

### 外部网络与 SSRF

书源远程导入、书源抓取、封面代理和调试请求必须走统一的 SSRF 防护网络层：

- 仅允许 `http`、`https`；
- 拒绝 URL 用户信息、回环、链路本地、私网、CGNAT、保留地址、Docker/容器网段、本机地址和云 metadata 地址；IPv4、IPv6 与 DNS 解析结果均要校验；
- 每次重定向重新校验，限制跳转次数；
- 限制连接/读取/总超时、响应大小、下载大小和并发数；
- 不转发服务端 Cookie、Authorization、代理凭据或云凭据；
- 不关闭 TLS 证书校验；
- 登录、注册、导入、调试和批量校验实施限流；
- 日志脱敏密码、Cookie、Token、Authorization 和敏感请求头。

## 前端与功能要求

保持可用、清晰、适配 PC 和手机浏览器。导航至少包含：`我的书架`、`搜书`、`书源管理`、`书架管理`、`分组管理`、`用户设置`，管理员额外可见 `管理后台`。

- 书源：本地文件/URL/JSON 导入、预览、URL 去重、覆盖/跳过、导入汇总、搜索筛选、批量启停/分组/权重/导出/删除/校验、隔离失败源、单源 SSE 调试；
- 书架：筛选、排序、类型标识、批量分组/刷新/缓存/清理/导出/删除、单书详情、来源失效标记与可解释换源；
- 分组：保留位掩码多选模型，一书可属于多个分组；
- 阅读：目录、正文、进度、书签、正文净化、图片失败回退、基础排版；
- 管理：用户状态、注册模式、邀请码、资源上限、系统状态、脱敏日志、备份状态；
- 所有加载、空状态、无权限、网络失败、部分成功和破坏性批量操作都有中文用户提示与明确范围。

前端升级必须渐进完成。可以参考 `LegadoTeam/legado` 的 API、行为和架构，但必须核对固定提交与许可证；禁止未经核验直接复制 Android/Web 模块、书源集合、图片或文档。

## 构建、测试、推送与发布规则

- 优先以 Docker 多阶段构建固定 JDK、Node、Gradle/npm/pnpm/Yarn 版本，避免要求用户在 Windows 或服务器手工安装开发依赖；
- 尽可能使用 GitHub Actions 完成 Linux 构建、测试、镜像构建和静态配置验证；
- 如果当前本机命令执行环境出现故障，先重试、切换合理的执行方式或让 CI 验证；不要把工具安装任务转给用户；
- 任何不能运行的验证必须说明真实原因，但仍应推进可实施的代码工作；
- 每次形成可部署成果后，使用清晰的提交信息提交并推送到 `origin`；
- 默认稳定发布在 `main`，日常开发可使用 `develop`，但用户获得的一键安装脚本必须默认安装经过验证的 `main`；
- 推送前保留 GPLv3、版权头、许可证与必要 NOTICE；建立第三方来源清单；
- 不能把“未运行测试”写成“测试通过”。

## 每次回复给用户的格式

用户不需要技术长报告。每次只用简体中文给出：

1. 已完成什么，GitHub 上对应的提交或发布链接；
2. 是否已经可以部署；
3. 可以部署时，只给一条完整可复制的 Debian 命令；
4. 运行该命令后，明确说“把终端全部输出原样发回来”；
5. 不能部署时，说明 Codex 正在继续完成什么，不要求用户安装开发工具或理解构建报错；
6. 涉及域名、DNS、已有数据覆盖等不可替代信息时，只问一个简单问题，并提供用户能直接回答的示例。

不要要求用户执行多条开发命令、设置 `JAVA_HOME`、安装 Node、切换 Node 版本、运行 Gradle、运行 yarn、检查 Docker 配置、阅读 YAML 或手动解决编译错误。

## 现在开始

1. 在 `F:\Code\reader-web-source` 检查现有 Git 状态，确保保留上游历史；
2. 将迁移基线和必要的新文件提交并推送到 `https://github.com/AdoreYL/reader-web.git`；
3. 实施第一个可部署版本：唯一 Compose 入口、容器内部 6788、反向代理、安装/更新/诊断脚本、健康检查、数据卷、备份、中文 README 和 CI；
4. 自行测试能够测试的项目；无法本地验证的部分交给 GitHub Actions 验证；
5. 推送后给用户一条 Debian 安装命令，并等待用户粘贴完整输出；
6. 根据服务器真实日志继续修复、推送和给出下一条命令，直到服务可稳定安装运行。
